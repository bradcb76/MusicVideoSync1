# Emby setup

Set `EMBY_ENABLED=true`, `EMBY_URL`, `EMBY_API_KEY`, and `EMBY_LIBRARY_ID` in `.env`.
Test the connection and verify the path mapping before automatic refresh. Emby outages are non-fatal.
