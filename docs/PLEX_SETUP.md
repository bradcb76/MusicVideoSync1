# Plex setup

Set `PLEX_ENABLED=true`, `PLEX_URL`, `PLEX_TOKEN`, and the selected library ID in `.env`.
Use connection testing and library discovery before enabling refresh. `PLEX_PATH_PREFIX` is the
path Plex sees for the organised video directory. Tokens are never returned or logged.
